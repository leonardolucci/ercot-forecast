[project]
name = "ercot-forecast"
version = "0.1.0"
description = "Probabilistic day-ahead price forecasting and battery dispatch for the ERCOT market"
readme = "README.md"
authors = [
    { name = "Leonardo Lucci" }
]
requires-python = ">=3.13,<3.14"
dependencies = []

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
