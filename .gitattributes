# Line endings are a property of this repository, not of the machine.
# Text files are stored and checked out as LF everywhere.
* text=auto eol=lf

# Exceptions: Windows batch interpreters require CRLF.
*.bat text eol=crlf
*.cmd text eol=crlf